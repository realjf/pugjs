doctype html
html
    head
        link(rel="stylesheet" href="style.css")
        style.
            p {
                color: red;
                text-decoration: underline;
            }
    body
        - const pStyles = { "text-align": "center", "text-transform": "uppercase"}
        p(pStyles) this is an html file!

